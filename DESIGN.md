# Design

## Source of truth

- Status: Active
- Last refreshed: 2026-08-02
- Primary product surfaces: 无限画布、Zodiac、工作台、我的资产、模型与渠道设置
- Evidence reviewed: `README.md`、`AGENTS.md`、`docs/zodiac-harness-architecture.md`、`docs/content/docs/overview/features.mdx`、`docs/content/docs/progress/todo.mdx`、`docs/content/docs/progress/pending-test.mdx`、`web/src/components/agent/zodic-panel.tsx`、`web/src/pages/canvas/project.tsx`、`web/src/lib/agent/zodiac-harness.ts`、`web/src/lib/agent/zodiac-run-events.ts`、`web/src/lib/canvas/canvas-resource-references.ts`、`web/src/lib/canvas/workflow-output-routing.ts`、`web/src/lib/canvas/canvas-agent-ops.ts`、`web/src/lib/canvas-theme.ts`

## Brand

- Personality: 克制、直接、可靠，像一位与用户并肩工作的创作搭档。
- Trust signals: 所有会改变画布或消耗模型额度的动作可见；运行状态、输入、产物和失败原因可追踪；本地优先；用户始终可以停止、重试、换版本或改为手动继续。
- Avoid: 把技术协议直接展示给普通用户；用聊天文案冒充实际执行；隐藏数据依赖；产物尚未保存就显示成功；把复杂工作流做成大量互不理解的新节点类型。

## Product goals

- Goals:
  - 把一句创作目标变成可见、可编辑、可运行的多模态工作流。
  - 同一张画布既支持边看结果边决定下一步，也支持确认后自动运行到完成或检查点。
  - 每次节点运行都能明确回答“用了什么输入、生成了什么、现在进行到哪里、下一步是什么”。
  - 上游成功产物稳定进入下游输入区和 `@` 菜单，不依赖 Zodiac 猜测内部引用格式。
  - 重跑保留历史版本，下游运行绑定到具体产物版本，结果可复现。
- Non-goals:
  - 不通过移除所有连线来模拟简单模式。
  - 不新增一个与图片、视频、文本、音频节点重复的通用“资产槽”节点类型。
  - 不让模型自行决定何时绕过用户确认或继续消耗额度。
- Success signals:
  - 用户能从任意生成步骤选择“逐步运行”或“自动运行”。
  - 自动运行只在当前产物已经持久化并可读回后启动依赖它的下游节点。
  - 逐步运行在每个检查点展示真实产物，并提供继续、修改、重跑、换版本和停止。
  - `配置 A → 图片结果 → 配置 B` 中，图片结果成功后立即出现在 B 的输入区及 `@` 菜单。
  - Zodiac 的执行卡不会在生成尚未结束时显示“已完成”。

## Personas and jobs

- Primary personas:
  - 探索型创作者：希望快速试做、比较结果，再决定是否继续。
  - 流程型创作者：已经确定步骤，希望一次运行完整流程并在少数关键点审核。
  - 工作流搭建者：希望把成功流程保存为可重复使用的模板或 Skill。
- User jobs:
  - 生成一个结果并马上查看、修改或分叉。
  - 将上一步结果无损交给下一步，而不手动重新上传或复制路径。
  - 一键运行完整流程，同时知道当前节点、用时、失败位置和已完成产物。
  - 重跑某一步并选择是否让下游从新版本继续。
- Key contexts of use: macOS 桌面端为主要运行环境；网页端用于预览和兼容；模型调用可能耗时较长、异步轮询、失败或只返回部分结果。

## Information architecture

- Primary navigation: 工作流、工作台、我的资产、提示词、Skills、文档、设置。
- Core routes/screens:
  - 画布：搭建节点、连接输入输出、查看产物与运行状态。
  - Zodiac：讨论、生成画布提案、启动工作流、展示运行账本和人工检查点。
  - 工作台：单次图片或视频创作与结果管理。
  - 我的资产：持久化产物及其来源信息。
- Content hierarchy:
  - 画布是工作流结构和当前结果的事实来源。
  - 运行账本是一次执行状态、输入快照、错误和重试关系的事实来源。
  - 资产存储是媒体文件和版本可用性的事实来源。
- Workflow execution model:
  - 拓扑决定“数据来自哪里、结果写到哪里”；运行策略决定“生成成功后是否自动继续”。二者不能混为一谈。
  - “逐步运行”在每个已配置检查点暂停；如果没有手动检查点，则默认在当前步骤产物就绪后暂停。
  - “自动运行”按依赖顺序运行，遇到检查点、需要补充配置、失败或用户停止时暂停。
  - 没有下游结果槽的单节点生成属于探索分支：创建一个新结果并停止，不猜测后续步骤。

## Design principles

- Principle 1 — 结果先于宣告: 只有产物写入本地存储、槽位更新且读回可用后，节点才能进入“完成”，下游才能变为“可运行”。
- Principle 2 — 一条连接，一份明确依赖: 连线必须对应可解释的数据依赖；可以弱化视觉重量，但不能用隐藏依赖或聊天记忆替代。
- Principle 3 — 一个节点身份，多次产物版本: 重跑不复制整个流程，也不覆盖历史；同一结果槽保留多个版本并明确当前版本。
- Principle 4 — 人工和自动共用同一执行器: 逐步运行只是自动执行器在检查点暂停，不维护第二套生成逻辑。
- Principle 5 — 输入显式可见、默认可用: 已连接且成功的上游产物默认进入下游输入区；`@` 用于点名、排序、固定版本或选择子项，不是让连线生效的隐藏前置条件。
- Tradeoffs:
  - 保留连线会比完全无连线更显结构，但能保证自动执行、复现和错误定位。
  - 使用现有媒体/文本节点作为结果槽，比新增通用槽节点更容易理解和预览，但需要增加清晰的空槽、版本和等待状态。

## Visual language

- Color: 延续 `canvasThemes` 的暖灰画布与蓝色焦点色；成功、等待、运行、失败只作为状态提示，不大面积染色。
- Typography: 中文正文清晰易读；运行状态和版本信息使用较小字号，但不得低于可读阈值；避免在产品中出现协议字段名。
- Spacing/layout rhythm: 画布控件保持低视觉重量；节点内的输入、结果和下一步动作按从上到下的自然阅读顺序排列。
- Shape/radius/elevation: 延续现有圆角面板；普通节点无额外厚重阴影；只有浮层、菜单和当前运行卡使用必要层级。
- Motion: 状态切换采用短时淡入和尺寸过渡；运行中的脉冲或进度动画应克制，并遵循减少动态效果设置。
- Imagery/iconography: 复用现有节点类型图标；空结果槽使用对应文本、图片、视频或音频图标与“等待结果”，不创造新的抽象资产图标体系。

## Components

- Existing components to reuse:
  - 文本、图片、视频、音频节点作为可预览的产物节点。
  - 配置与终端节点作为动作节点。
  - 现有连接线、节点状态、提示词组装器、`@` 菜单、Zodiac 会话面板。
- New/changed components:
  - 结果槽状态：现有产物节点增加明确角色，空时显示“等待上一步”，写入后直接显示当前版本。
  - 一键结果槽：动作节点提供“添加结果槽”；系统按当前生成类型创建并连接对应的文本、图片、视频或音频结果槽，用户不必手动画线。
  - 节点输入区：展示所有已连接输入及其类型、状态、当前版本；允许启用、停用、排序或固定版本。
  - 运行启动菜单：提供“逐步运行”和“自动运行”，并显示预计运行范围与检查点。
  - 运行账本：按节点展示排队、运行、产物保存、等待确认、完成、失败、跳过和停止。
  - 检查点动作：继续下一步、修改提示词、重跑本步、选择其他版本、从这里自动继续、停止。
  - 结果版本器：同一槽位内查看历次成功结果，选择当前版本，删除单个版本；正在被某次运行引用的版本不得静默丢失。
- Variants and states:
  - 结果槽：empty、waiting、running、persisting、ready、error、stale。
  - 动作节点：idle、ready、queued、running、waiting_for_review、completed、error、stopped、blocked。
  - 工作流运行：guided、automatic；两者共用相同节点状态和事件。
- Token/component ownership: 颜色和主题来自 `canvasThemes` / Ant Design token；运行状态组件属于画布与 Zodiac 共用层，不在单一页面复制实现。

## Accessibility

- Target standard: 桌面端核心流程按 WCAG 2.1 AA 的对比度和键盘可达性设计。
- Keyboard/focus behavior: 运行菜单、检查点、版本切换和 `@` 菜单均可通过键盘完成；浮层关闭后焦点回到触发节点。
- Contrast/readability: 状态不能只靠颜色表达，必须同时包含文字或图标；错误详情可展开查看。
- Screen-reader semantics: 节点状态、运行进度、当前版本及按钮使用明确标签；实时状态使用克制的 live region。
- Reduced motion and sensory considerations: 减少动态效果时禁用持续脉冲和大范围平移动画，仅保留即时状态更新。

## Responsive behavior

- Supported breakpoints/devices: macOS 桌面窗口优先；窄窗口下 Zodiac 面板、节点浮层和运行账本不得遮住主要操作。
- Layout adaptations: 宽屏显示画布与 Zodiac 并排；窄屏将运行账本收进可展开面板，节点上的“下一步”保留。
- Touch/hover differences: 核心操作不能仅依赖 hover；hover 工具条对应操作也应在选中节点或上下文菜单中可达。

## Interaction states

- Loading:
  - 文本输出实时写入目标结果槽。
  - 图片、视频、音频在无法展示部分内容时显示真实排队、生成、下载和保存阶段。
  - Zodiac 运行卡跟随执行器事件，不用模型回复文本推断进度。
- Empty:
  - 连接在动作节点后的空产物节点显示“等待上一步”，并说明预期类型。
  - 空槽可参与流程结构，但不能作为下游已就绪输入。
- Error:
  - 错误归属到具体节点和尝试；其依赖下游标记为“等待上游”，不误显示成功。
  - 默认只停止受影响分支；用户可重试本步、换模型、修改输入或停止全部。
- Success:
  - 先保存产物和输入快照，再提交结果槽当前版本，最后发布“产物已就绪”事件。
  - 逐步模式进入检查点；自动模式把满足依赖的下游节点加入队列。
- Disabled:
  - 上游为空、失败、类型不兼容或配置缺失时，下游运行按钮禁用并给出可行动原因。
- Offline/slow network:
  - 已开始的异步任务保持可恢复的任务标识；应用重新打开后能继续查询或明确标为中断，不能永久停留在“运行中”。
- Upstream input and `@` behavior:
  - 直接连接到当前动作节点的成功产物全部进入输入区和 `@` 菜单。
  - 提示词没有 `@` 时，默认使用所有已启用的连接输入；不能因为存在普通提示词而清空输入。
  - 出现 `@` 时，按显式引用顺序组织输入；未引用但仍启用的连接输入保持可见，并由用户决定是否关闭。
  - UI 显示节点标题和缩略图；内部使用稳定节点、槽位及产物版本身份，不把“图片 1”当作持久标识。
  - 每次运行开始时，把“最新成功版本”等动态选择解析成具体版本并写入运行快照。

## Content voice

- Tone: 简短、直接、以当前结果和下一步为中心。
- Terminology:
  - 面向用户使用“结果槽”“逐步运行”“自动运行”“检查结果”“继续下一步”“当前版本”。
  - 内部可使用 artifact、binding、DAG、run ledger，但这些词不进入普通产品界面。
- Microcopy rules:
  - 不写“已应用”来代替“正在生成”。
  - 不写笼统的“处理失败”；优先写清失败步骤和可采取的动作。
  - 不解释为某个 Agent 或协议服务的技术限制，除非用户展开错误详情。

## Implementation constraints

- Framework/styling system: React 19、TypeScript、Ant Design、Tailwind、Zustand、Tauri 2；复用现有画布主题与存储适配层。
- Design-token constraints: 不新增另一套画布颜色；不在页面组件中硬编码浅色/深色分支。
- Performance constraints:
  - 高频流式事件不得让整张画布重渲染。
  - 运行账本按 run 和 node-run 分片订阅；媒体正文不复制进会话状态。
- Compatibility constraints:
  - 桌面端是最终验收环境；网页端通过不能替代桌面端验证。
  - 资产、运行账本、版本和输入快照写入原生应用数据目录。
  - 工作流正文和产品界面不得暴露本机绝对路径。
- Execution contract:
  - 单节点生成函数必须返回可等待的结构化结果，不能以 `void` 并发触发多个依赖步骤。
  - 新增统一拓扑执行器；`run_generation` 只运行一个节点，完整流程使用一个由执行器管理的工作流运行命令。
  - 执行器只根据结构化节点状态和持久化结果推进，Zodiac 不直接编排定时器或轮询循环。
  - 一个动作节点的一个结果只能写入一个声明结果槽或创建一个探索结果，不能同时发生。
  - 结果槽必须显式标识，不再仅以“类型碰巧匹配的第一个下游节点”推断。
  - 连接必须区分结果写入与输入引用，并校验端口方向、媒体类型和单一写入者；禁止 `config → config` 这类无法传递产物的动作互连。
  - Zodiac 创建多步流程时必须使用结构化输入绑定和正确的结果槽，不得自行拼接 `@[node:id]`、按数组顺序硬连节点或把“文本结果槽”“视频结果槽”识别为配置节点。
  - 多结果作为同一次槽位版本的结果集合处理；下游可引用整个集合或其中选定项。
- Test/screenshot expectations:
  - 单元测试覆盖输入解析、槽位选择、拓扑排序、检查点、失败阻断、重跑版本和恢复。
  - 集成测试覆盖 `配置 A → 图片结果 → 配置 B → 视频结果` 的逐步与自动运行。
  - 验证 `@` 菜单在上游成功、重跑、失败、删除版本及重新打开应用后的表现。
  - 打包桌面端验证真实流式状态、停止、恢复和资产可读回。

## Open questions

- [ ] 自动运行遇到独立分支失败时，默认继续其他分支还是停止全部；建议默认继续不依赖该失败节点的分支，并在运行摘要中明确部分失败。
- [ ] 首次启用自动运行时是否需要展示预计调用次数和可能消耗；建议在超过一个付费生成节点时展示一次简短确认。
- [ ] 结果槽的历史版本默认保留数量；建议先不自动删除，由用户手动删除单个版本，后续再增加存储清理策略。
- [ ] 检查点放在动作完成后还是结果槽上；建议产品操作放在结果槽上，执行策略存于连接后的动作边界。
