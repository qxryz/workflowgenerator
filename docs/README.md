# WorkflowGenerator 文档站

文档站使用 Next.js 与 Fumadocs，内容位于 `content/docs/`，入口索引位于 `index.md`。

```bash
bun install
bun run dev
```

检查类型并生成内容索引：

```bash
bun run types:check
```

构建生产版本：

```bash
bun run build
```
