---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

本文档用来记录当前项目后续比较值得处理的事项。

- DSH 插件后续：当前内嵌 Launcher 只额外监控并按用户确认安装 dsh-market，不接管市场中的插件列表、安装或配置；继续使用 DSH 自己的 bundle 与 profile 机制开发和试玩插件，在插件数量形成稳定工作流后，再评估独立的“我的 DSH”Profile 管理，不把 DSH 插件并入 Workflow Generator 插件系统。
- 作者私藏发布源：应用端目录协议、本地原生浏览、分类安装、目录签名和完整性校验已接入；建立作者私有内容仓库后，按开发文档配置 GitHub Actions，只将确认发布的 `catalog.json`、`catalog.json.sig` 与内容文件推送到 `author-library-dist`，不得把草稿、私有历史或写入凭据带入公开分发分支。
- Zodiac 后续：完善多模型工具调用适配与跨画布工作流提案复用。
- 导演台后续：原项目 UE 骨骼人体及其独立许可文件按用户要求继续保留在仓库中，README 底部保留作者、来源和 Sketchfab Standard License 说明。后续另行设计用户自有 GLB / FBX 动画片段、播放控制、骨骼重定向与 Zodiac 场景语义操作；原上游项目没有可用的时间轴或动画导入实现，不以空按钮代替。
- 模型适配器闭环：让渠道与模型选择的 adapter 真正进入请求构造、鉴权和流式解析，接通 OpenAI Responses 与 Anthropic Messages；合并重复的模型目录，并让参数文档、工作台 schema 与请求序列化共用同一份模型定义。
# 音频能力后续

- 增加 Qwen3-ASR-Flash-Filetrans 长音频异步转录（12 小时 / 2 GB）与任务恢复。
- 增加实时语音合成、实时转录和录音输入；首版优先保证非实时创作任务稳定落盘。
- 在获取到真实账户的完整音色清单后，为 Qwen Audio TTS 增加可试听的音色库和收藏。
