# AGENTS.md

本文档用于约束本项目中的 AI / 自动化开发行为。开发时优先遵循本文件，其次遵循用户当前消息。

## 基本原则

- 先读现有代码，再动手修改，优先沿用项目已有结构和写法。
- 写代码保持最少行数，能简单实现就不要引入复杂抽象。
- 标准格式、协议、解析、压缩、加密、日期等通用能力优先使用成熟稳定的库，不要手写底层实现，除非用户明确要求或项目已有实现必须沿用。
- 不要为了“兼容更多场景”写大量分支，只实现当前明确需要的功能。
- 项目尚未上线，不需要兼容旧数据；本地存储结构调整时直接按新设计修改，不写旧字段兼容或数据迁移兜底，除非用户明确要求。
- 每次写完代码都要做与风险匹配的语法、类型和测试检查；只要改动影响应用可感知行为，还必须按“桌面端构建与核验”生成最新版桌面端供用户核验。
- 每次修改都必须检查关联项：同步核对同类入口、共享数据模型、持久化与恢复、导入导出、删除清理、桌面关闭刷盘及对应测试，不能只验证当前按钮或单一路径。
- 不要改无关文件，不要顺手重构。
- 如果工作区已有用户改动，不要回滚，不要覆盖；只在必要范围内追加修改。

## 反复提醒沉淀

- 如果开发过程中总是遇到某个问题，或者用户反复提醒同一个注意事项，需要把该注意事项补充到本文件。
- 补充时写成明确、可执行的规则，避免只写模糊描述。
- 新规则应放到最相关的章节；找不到合适章节时放到“项目注意事项”。

## 前端规范

- 前端使用 Vite、React、React Router、TypeScript、Ant Design、Tailwind、Zustand。
- 编写 Ant Design 相关代码时，参考 https://ant.design/llms-full.txt 理解组件 API、示例和设计规范，并优先结合项目当前 antd 版本与既有写法。
- 外部服务请求统一放在 `web/src/services/api/`，由浏览器前端直连，不假设存在项目后端。
- 全局或跨页面状态优先放在 `web/src/stores/`。
- 已经放在全局 store 或全局 hook 中的状态/动作，组件需要时直接使用对应 store/hook，不要为了“纯组件”层层透传 props；避免一个组件传递过多参数。
- 全局组件、全局常量、全局配置等全局性质的内容不要作为 props 或参数层层传递；哪里需要就在哪里直接从对应全局入口获取。
- 多个页面重复出现的 UI 副作用动作，例如复制文本并提示、下载并提示、统一确认弹窗，优先抽成 `web/src/hooks/` 下的全局 hook；不要放进 store，除非它确实是需要共享/订阅的状态。
- 路由页面放在 `web/src/pages/`，页面布局放在 `web/src/layouts/`，路由配置放在 `web/src/router.tsx`。
- 画布页面放在 `web/src/pages/canvas/`，画布组件放在 `web/src/components/canvas/`，画布状态放在 `web/src/stores/canvas/`，画布工具函数放在 `web/src/lib/canvas/`。
- 页面按目录组织，例如 `web/src/pages/image/index.tsx`；页面里只有一个主业务组件时直接写在对应页面入口中，不要单独拆 `Manager` 组件再传一堆 props。
- 不要新增只做简单转发的组件，例如只 `return <X>{children}</X>` 或只换个名字透传 props；直接在使用处使用真实组件或把逻辑写进当前文件。
- 页面私有 hook 放在对应页面目录下，例如 `admin/assets/use-admin-assets.ts`；只有多个页面真实复用的 hook 才放到外层 `hooks/`。
- 管理后台页面私有组件放到各自页面目录的 `components/` 下，例如 `admin/assets/components/`、`admin/prompts/components/`；不要为了单页面使用放到 `admin/components/` 共享目录。
- 管理后台主题、背景、卡片阴影、表格配色等统一在 `web/src/lib/app-theme.ts`、`AppProviders` 或必要的全局 CSS 作用域中配置；页面私有组件不要自己写 `dark ? ...` 主题分支。
- 组件优先使用函数组件和现有 hooks，不新增大型状态管理方案。
- UI 图标优先使用 `lucide-react` 或项目已经使用的 Ant Design 图标。
- 页面文案保持中文。
- 复杂生成与工作流界面必须使用渐进式呈现：默认每个区域只保留当前阶段的一个主要决定，输入与提示词、模型参数、版本历史和自动化策略按需逐层展开；不要靠重复提示、技术解释或多个同义入口堆叠体验。
- 不要在组件里堆太多无关逻辑；复杂逻辑优先抽成同目录工具函数或小组件。
- 样式优先由组件自己管理；组件私有样式优先使用 Tailwind className 或少量内联 style，不要为单个组件新增大量全局 CSS。
- 全局 CSS 只放基础变量、全局重置、跨页面通用样式和少量第三方组件必要覆盖；不要在 `globals.css` 堆页面私有样式。
- 代码尽量短小直接，少拆不必要组件，少做多层 props 传递，避免为了抽象堆出更多代码。
- 桌面端业务数据统一通过原生存储适配层写入应用数据目录；`localforage` 只作为网页预览回退和旧数据迁移来源，`localStorage` 只用于极小、可丢失的界面状态，不要用来保存业务列表、生成记录、图片、base64、大 JSON 或凭据。
- 桌面端工作台的本地媒体读取、下载、保存资产和加入参考素材，禁止依赖网页下载器、`fetch(wg-media://...)` 或自定义协议页面跳转；必须通过 Tauri 原生命令或原生存储适配层处理。相关修复必须用打包后的桌面应用验证，网页端通过不能替代桌面端验证，尤其要防止 WebKit 出现 `Load failed` 后让整组结果操作失效。
- 桌面端内置模型供应商的创建任务、生成和轮询请求不得直接依赖 WebView 的 `fetch`/`axios` 跨域能力；必须通过可返回真实 HTTP 错误的 Tauri 原生网络适配层。新增供应商时要同时验证网页端和打包后的桌面端，不能把 CORS 导致的 `Load failed` 当作供应商或 Key 错误。
- 工作台只有在生成媒体和生成记录都已持久化成功后，才能把结果标记为“成功”并开放下载、保存资产等操作；“重新生成”必须追加到原生成记录，不能覆盖旧结果或悄悄创建新记录。资产保存必须等待持久化完成并读回确认，不能仅更新内存状态就提示成功。

## 模型工作台与原生 UI 路线

- 新增或调整模型工作台时，必须先在实现说明和契约测试中明确选择下列一条路线；不能只写“支持原生 UI”。应用内的模型条目只展示已适配的推荐模型、可用入口、实际参数和官方来源；架构说明、路线编号、组件归属与 Zodiac 技术说明保留在独立“参考文档”章节，不与模型条目或推荐模型数量混算。
- 路线一（数据驱动的通用面板）：适用于普通选项、开关、数字、尺寸、分组等声明式参数；在模型目录声明 `parameters`，由 `ModelParamPanel` 按 schema 渲染。没有特殊上传、多阶段请求、轮询、条件序列化或专属交互时优先走路线一。
- 路线二（代码编写的原生面板）：适用于特殊文件输入、多阶段调用、轮询任务、模型专属约束、参数联动或明显不同的交互；由专门页面/组件实现，并配套独立请求契约和测试。
- 当前千问音频工作台明确走路线二：`AudioPage / AudioInspector` 负责语音生成、声音复刻和语音转录；三个任务分别绑定 Qwen-Audio-TTS、Qwen 声音复刻与 Qwen-ASR OpenAI 兼容接口。
- 当前 MiniMax 拆成 `MiniMax Token Plan（原 Coding Plan）` 与 `MiniMax API 计费` 两个独立厂商/渠道，并分别固定使用 `minimax-token-plan-native` 与 `minimax-api-native` 两个内部适配器；每个渠道只能保存自己的一组 Base URL 与 Key，两种 Key、适配器身份和计费状态不得混用，也不得自动切换或回退到另一厂商。固定内部适配器不是用户选项：MiniMax 渠道表单不得显示只有一个选项的适配器下拉。Token Plan Key 必须按官方规则识别为 `sk-cp` 前缀，API 计费 Key 必须识别为 `sk-api` 前缀；`/models` 能返回目录不等于 Key 类型或计费线路正确，不能再把模型列表响应当作凭据类型验证。所有厂商的模型展示与调用遵循同一规则：服务端返回的模型必须原样完整显示，不得使用本地目录、能力表、套餐白名单或权益接口进行二次过滤或调用门禁；本地目录只负责初始推荐和原生工作台映射，具体权限、额度和错误由厂商接口返回。渠道编辑页的“推荐模型”必须保持精选，只展示已经适配、具有代表性的当前模型；它与“选择模型”及接口返回的完整列表是两个独立概念，不得为了完整展示而把旧版本全塞进推荐区。模型选择弹窗必须把“推荐模型”“模型目录”“接口返回”“已添加”分开显示，接口拉取只刷新“接口返回”，不得把来源混入内置推荐或目录。两个适配器可以复用 MiniMax 底层 endpoint、序列化与响应解析函数，但不得读取对方凭据或自动回退。MiniMax 渠道 UI 不得显示只有一个选项的适配器下拉，也不得显示套餐权益检测、手动权益开关或独立权益卡片。M3 不属于专属工作台：它复用现有会话 UI，并通过 MiniMax Anthropic Messages 原生序列化器发送请求。MiniMax 图片、视频与音频工作台走路线二：Token Plan 和 API 计费目录均完整展示 MiniMax 已接入模型，各级套餐的实际模型权限和额度以 MiniMax 接口返回为准。MiniMax 推荐区只放 M3、Image 01、H3、Hailuo 2.3 / Fast 与 Speech 2.8 HD / Turbo；M2 系列仍保留在完整模型选择中。Image 01 使用同步图片接口，H3 使用 V2 创建与轮询接口，Hailuo 使用 V1 创建、查询、文件取回三段接口，Speech 2.8 使用语音生成接口，快速声音复刻使用文件上传与复刻接口；MiniMax 音乐生成和语音转录不纳入当前工作台。
- Grok、Agnes、Seedream、Seedance 等已有专属经验的图片/视频面板走路线二；其余没有特殊交互的目录模型走路线一，未声明专属 schema 时才回退到按能力生成的通用面板。
- Seedance 2.5 明确走路线二，并同时提供三个用户入口：与图片/视频/音频并列的 `SD2.5` 专属工作板、视频工作台原生面板、工作流画布视频节点。三处共用 `seedance25TaskMode` / `seedance25Continuation` / `seedance25OutputFormat` 契约和方舟 `/api/v3/contents/generations/tasks` 请求序列化，官方模型 ID 为 `doubao-seedance-2-5-260628`。生成与延长仅支持 4–30 秒或 `-1` 智能时长，编辑必须发送 `duration: -1`；编辑/延长固定 `ratio: adaptive`。输出仅支持 480p/720p 与 mp4/mov，不得显示 60/90 秒、1080p 或 4K。单次最多 30 张图片、10 个视频、10 个音频且总素材不超过 50，参考视频和参考音频各自总时长不超过 30 秒；编辑主原片须为 4–30 秒。参考视频只允许 https 公网 URL 或 `asset://` 方舟素材 ID，不得把本机视频转成官方未声明支持的 Base64；请求体不得超过 64MB。可见参数与限制必须以 Seedance 2.5 官方说明为准。
- 每个新工作台必须同时记录并核对：选择路线一或路线二、负责的 schema 或组件、实际 HTTP endpoint、鉴权方式、请求序列化器、每个可见控件对应的请求字段、条件显示/省略规则、输入文件限制和返回结果解析方式。
- 全局偏好必须写清作用范围和复制时机：新任务默认模型可被工作台最后选择更新，只用于下一次创作和新建节点，不得覆盖已有节点；模型专属参数必须按模型能力显示和归一化，不能用一个通用枚举跨厂商静默替换。Zodiac 角色、图片提示前缀、视频提示、音频指令和工作流节点提示词必须独立保存，禁止重新合并为含义不明的全局系统提示词。
- 音色等厂商枚举必须随当前模型展示对应厂商的真实预设，不能跨厂商共用同一列表；厂商支持克隆或设计音色时，应在预设下拉中保留“自定义”入口，并把用户填写的 Voice ID 原样用于请求。
- UI、应用内文档和真实请求必须共用或引用同一份模型契约；任何没有发送到请求体、请求头或任务流程的控件，不得显示为已支持。模型不支持的参数必须隐藏或明确禁用，不能让用户修改后静默无效。
- 多阶段模型必须在 UI 和文档中写清调用顺序与绑定条件。例如声音复刻先创建 voice，再用完全相同的 `target_model` 合成；切换到生成页时必须保留该模型，不能自动换到其他 TTS 模型。
- 接口实现完成后至少增加契约测试，覆盖 endpoint、请求字段名、枚举映射、必填/条件参数和模型绑定关系；只验证面板能显示不算完成。

## 画布 UI 规范

- 做 canvas 前端 UI 时必须遵循当前画布主题。
- 优先使用 `canvasThemes`、`useThemeStore` 或 Ant Design `ConfigProvider` token。
- 不要硬编码黑白、stone、slate 等颜色导致浅色/深色主题不一致。
- 新增画布按钮、弹窗、浮层时，尽量复用已有工具栏、节点面板、Modal 的视觉风格。
- 画布顶部工具栏和状态信息优先采用极简扁平风格：无边框、无阴影、无胶囊背景，融入整体背景，弱化按钮感，仅保留轻微 hover 反馈，保持简洁现代、低视觉重量。
- 左侧画布面板等列表里的节点/元素缩略图容器，非图片类型（文本、配置、视频、音频等）不要使用 `theme.node.fill`（`#e7e5df`/`#292524`）这类灰色背景，图标直接无背景展示，尽量不要给多余底色，保持干净。
- 画布内的操作按钮（如面板里的「添加」「导出」「选择」等）默认用扁平无底色样式：透明背景、仅 `hover:bg-black/5 dark:hover:bg-white/10` 轻微反馈，靠图标+文字表达，不要用 `theme.toolbar.activeBg`（`#e7e5df`/`#3a3631`）或 `theme.node.fill` 之类的灰色作为按钮填充底色。灰色 `activeBg` 只允许用于「选中态」等需要表达状态的高亮，不要当普通装饰底色。
- 图片节点尺寸逻辑要尊重原始比例，除非功能明确要求自由变形。
- 批量生成、多图展示、助手面板等画布交互要尽量简洁，不要占用过多画布空间。
- 交互式插件节点默认是数据或创作容器，不等于工作流可执行动作；没有补齐编译、预检、执行、回执与失败恢复契约前，不得把它描述成可由全局运行器自动执行，也不得接受 `run_generation`。
- 大型工作台同时提供一级页面与画布插件节点时，两处必须共享同一份按实例隔离的原生持久数据；节点只负责投影、输入输出和打开入口，不得再复制一份独立业务真源。

## 文档规范

- 软件对外定位固定为“qxryz 自用工具箱”，不要包装成企业平台、万能 AI 产品或面向所有人的商业服务；产品界面仍然从实际使用者角度写，不出现开发目的、内部实现或“这是给作者做的”之类说明。
- README 使用作者本人说话的短句和第一人称语气，不写宣传腔、AI 总结腔或大段原理。只保留：一句自用工具箱说明、从 Tags 下载的快速体验、Gatekeeper 处理方法、简短功能列表、致谢仓库和 AGPL 许可证强调。
- README 不放开发环境、安装依赖、源码构建、项目结构、架构图、分发分支或详细文档目录；这些内容只写在开发文档和本文件中。
- `docs/index.md` 放给 AI 使用的文档索引，不要再放到 `docs/content/docs/` 内容目录里。
- 详细功能介绍写到 `docs/content/docs/overview/features.mdx`。
- 后续待办写到 `docs/content/docs/progress/todo.mdx`。
- 已实现但还需要用户测试确认的事项写到 `docs/content/docs/progress/pending-test.mdx`。
- `docs/content/docs/progress/pending-test.mdx` 用来记录这个版本实际做了哪些可测试变更；`CHANGELOG.md` 的 `Unreleased` 只保留对这些变更的版本级归纳，避免逐条照搬实现细节。
- 每次重大改动（新增/调整/删除功能、接口或工具，影响用户可感知行为）完成后，都要在 `CHANGELOG.md` 的 `Unreleased` 追加一条记录，按 `[新增]` / `[调整]` / `[修复]` / `[优化]` 前缀分类，用一句中文归纳；纯内部重构、格式化、无用户可感知影响的小改动可不记。
- 每次 todo 事项完成后，先从 `docs/content/docs/progress/todo.mdx` 移到 `docs/content/docs/progress/pending-test.mdx`，不要直接写进正式功能说明；用户确认测试通过后再更新 `docs/content/docs/overview/features.mdx`。
- 每次任务完成前，都要根据实际变更检查并更新 `docs/content/docs/progress/todo.mdx` 和 `docs/content/docs/progress/pending-test.mdx`；如果功能或待办没有变化，也要确认无需修改。
- 文档不要写过期日期；除非用户明确要求记录具体时间。

## 发版本流程

- 发版本时，先把 `CHANGELOG.md` 的 `Unreleased` 变更整理成新的版本记录，并保留空的 `Unreleased` 标题。
- 按当前版本号提升一个版本；根目录 `VERSION` 使用 `vX.Y.Z`，`web/package.json`、`web/src-tauri/Cargo.toml` 与 `web/src-tauri/tauri.conf.json` 使用 `X.Y.Z`，四处语义版本必须一致。
- 将当前未提交的代码全部提交到 Git。
- 提交完成后，给当前提交打最新版本号对应的 tag，例如 `v0.0.5`。
- 推送 `v*` tag 后由 `.github/workflows/release-desktop.yml` 构建 macOS 安装包、更新包和 `latest.json`，并挂到同一个 GitHub Release；README 的快速体验和软件内更新都只认这个仓库的 Tags / Releases。
- 发版本前执行类型检查、相关测试、完整测试和桌面端构建核验；构建规则见下一节。

## 应用更新与签名

- 软件内检查更新固定放在“设置 → 软件更新”，不得占用顶部导航；只使用 `qxryz/workflowgenerator` 的 GitHub Release，不得再指向上游仓库。入口应显示当前版本，发现更新后由用户点击下载并安装，完成后自动重启。
- 更新安装前必须调用桌面刷盘流程，等待画布、Zodiac 会话、导演台、配置和媒体写入完成；保存失败时不得继续替换应用或重启。
- Apple Developer ID 与 Tauri 更新包签名是两件事。当前没有 Apple Developer ID，也不做公证，macOS 应用使用 `signingIdentity: "-"` 的 ad-hoc 签名；README 必须保留 Gatekeeper 的右键打开、隐私与安全性放行及可信来源隔离属性处理方式。
- Tauri updater 的更新包校验签名不能留空或关闭。公钥可以提交到 `tauri.conf.json`，私钥只允许保存在被忽略的 `web/.local-secrets/` 和 GitHub Actions Secret `TAURI_SIGNING_PRIVATE_KEY`；加密私钥的密码只允许放在 Secret `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`，二者都禁止写入源码、日志、Release 或提交记录。
- 更新私钥必须长期备份；丢失后，已经安装旧公钥版本的用户将无法继续自动更新。更换密钥必须先设计公钥轮换版本，不能直接覆盖。
- 正式推 tag 前必须确认 GitHub Actions 已配置 `TAURI_SIGNING_PRIVATE_KEY`，加密私钥还要配置 `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`；在 Release 生成后核对 DMG、macOS updater 压缩包、`.sig`、`latest.json`、校验和与构建证明均存在且签名匹配。

## 桌面端构建与核验

- 每次完成用户可感知的应用更新后，都要构建当前源码对应的最新版桌面端供用户核验；不需要等待用户另行提醒。
- 桌面构建使用项目既有 `npm run desktop:build` 流程。构建成功后检查 `.app` 的版本、架构和包结构，执行签名校验，并实际启动一次完成冒烟检查。
- 用户未要求正式发版时，不提升版本号、不打 tag、不提交、不生成 Release 压缩包；只更新当前版本的本地桌面端 `.app`。
- 标准核验产物只保留最新的 `web/src-tauri/target/release/bundle/macos/WorkflowGenerator.app`。新构建验证通过后，删除项目标准输出位置中的旧 `.app`、旧 ZIP、旧 DMG、旧 PKG 和旧 `release/` 产物，不得并存多个版本。
- 清理前必须先确认新 `.app` 已成功构建、签名校验通过且能够启动；不得删除源码、用户数据、非标准目录中的用户文件或尚未被新构建替代的唯一可用桌面端。
- 如果机器没有 Apple Developer ID，使用 ad-hoc 签名进行本地核验，并在交付说明中明确“可本地使用但未公证，不属于公开分发版本”；不得把 ad-hoc 签名描述成正式发布签名。
- 最终交付必须给出最新 `.app` 的绝对路径，并说明类型检查、测试、签名、启动检查和旧产物清理结果。

## 项目注意事项

- 仓库内所有运行时下载、发布、文档、容器和 GitHub 操作地址统一使用 `qxryz/workflowgenerator`；`basketikun/infinite-canvas` 只允许出现在 README 致谢或第三方来源声明中。不存在的目录、插件和发布链路不得继续保留工作流或操作文档。
- 面向用户、Zodiac 或终端 Agent 的文案和数据协议不得要求、展示或复制本机绝对路径；工作流文件统一使用相对路径、文件名或 `WG_INPUT_DIR` / `WG_OUTPUT_DIR` 等环境变量。绝对路径仅允许在桌面端内部完成文件访问，不得进入工作流正文和产品界面。
- 桌面端的画布、Zodiac 会话、配置和媒体保存在本机应用数据目录；旧 WebView 数据只作为一次性迁移来源。WebDAV 仍是用户自行配置的跨设备同步，不要误写成账号云同步。
- 当前 AI API Key 保存在本机应用数据库中，不使用系统钥匙串，也没有静态加密；桌面端内置厂商请求通过 Tauri 原生网络适配层发出，网页预览才由浏览器直连，涉及安全说明时要写清楚。
- Docker 静态资源路径目前仍是待办项，文档中不要过度承诺生产部署已经完全验证。
- 引入开源 3D、图片、字体或音频项目时必须分别核对源码与每个素材文件的再分发许可，保留要求的版权声明；仓库代码使用宽松许可证不代表同仓模型素材也可随桌面应用打包，许可不清或禁止独立分发的素材不得进入构建产物。
- 用户明确要求保留在仓库中的第三方内容不得擅自改为本机私有文件、加入忽略或从 Git 移除；当前 UE Mannequin 模型及其独立许可文件继续由仓库跟踪，并在 README 底部保留来源与许可证说明。
- 作者私藏目录与官方插件目录属于发布者控制的远程代码或内容入口，正式版必须先验证发布目录签名；官方插件还必须校验清单中的文件哈希，校验失败只能回退到应用内置内容。
